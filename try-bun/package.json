{
  "name": "try-bun",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot",
    "watch": "bun run --watch",
    "start": "bun run"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}